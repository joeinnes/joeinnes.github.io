# joeinnes.github.io

Personal portfolio index page. Lists Joe's public projects with descriptions and links.

Single-file static site — pure HTML and CSS, no build step, no dependencies.

## Editing

Edit `index.html` directly. To add a new project, follow the skill at `personal/.claude/skills/joeinnes-portfolio/SKILL.md`.
